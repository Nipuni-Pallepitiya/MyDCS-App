<?xml version="1.0" encoding="utf-8"?>



<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/custom_toast_layout"
    android:orientation="vertical"
    android:background="#929095" >

    <ImageView
        android:id="@+id/custom_toast_image" android:layout_width="wrap_content" android:layout_height="wrap_content" android:contentDescription="Hello world" />

    <TextView
        android:id="@+id/custom_toast_message"
        android:layout_width="256dp"
        android:layout_height="46dp"
        android:contentDescription="To"
        android:shadowColor="#929095"
        android:text="@string/NToast"
        android:textColor="#F7F6FA"
        android:textSize="18sp" />

</LinearLayout>
